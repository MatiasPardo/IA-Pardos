version: '3.8'

services:
  ollama:
    build:
      context: ./assistant
    container_name: pardos-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    networks:
      - pardos-network

  pardos-backend:
    build:
      context: ./sistemPardo/backend
    container_name: pardos-backend
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - OLLAMA_URL=http://ollama:11434
    depends_on:
      - ollama
    networks:
      - pardos-network

  frontend:
    image: nginx:alpine
    container_name: pardos-frontend
    volumes:
      - ./sistemPardo/frontend:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - pardos-backend
    networks:
      - pardos-network

  portainer:
    image: portainer/portainer-ce:latest
    container_name: pardos-portainer
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    ports:
      - "9000:9000"
    networks:
      - pardos-network

volumes:
  ollama_data:
  portainer_data:

networks:
  pardos-network:
    driver: bridge